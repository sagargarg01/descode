<footer>

    <% if(locals.user && locals.user.verifyUser === true){ %>
        <div class="top"></div>
    <% } %>

    <div class="inner">
        <div class="copy">
            Web Development Project
        </div>

        <div class="container-footer">
            <div class="home">
                <a href="/"> Home </a>
            </div>
            <% if(locals.user && locals.user.verifyUser === true){ %>
            <div class="profile">
                <a href="/users/profile/<%= locals.user.id %>"> Profile </a>
            </div>
            <% } %>

        </div>
    </div>


</footer>